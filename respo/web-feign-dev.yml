server:
  port: 8766

spring:
  application:
    name: web-feign
  boot:
    admin:
      client:
        url: http://localhost:9411

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true